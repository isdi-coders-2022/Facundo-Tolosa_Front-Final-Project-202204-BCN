import FooterContainer from "./FooterStyles";

const Footer = (): JSX.Element => {
  return (
    <FooterContainer>
      <button>{"<<"}</button>
      <p>0</p>
      <button>{">>"}</button>
    </FooterContainer>
  );
};

export default Footer;
